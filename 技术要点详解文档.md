# 家庭常备药品智能管理系统 - 技术要点详解文档

---

## 目录

1. [JWT身份认证机制](#一jwt身份认证机制)
2. [家庭权限隔离机制](#二家庭权限隔离机制)
3. [定时任务调度](#三定时任务调度)
4. [微信订阅消息推送](#四微信订阅消息推送)
5. [数据库触发与变更日志](#五数据库触发与变更日志)
6. [前端数据缓存](#六前端数据缓存)
7. [OCR文字识别与药品溯源](#七ocr文字识别与药品溯源)
8. [JSON类型字段处理](#八json类型字段处理)
9. [密码加密存储](#九密码加密存储)

---

## 一、JWT身份认证机制

### 1. 基本概念

JWT（JSON Web Token）是一种开放标准（RFC 7519），用于在网络应用环境中安全地传输信息。

### 2. JWT 结构

JWT 由三部分组成，用点（`.`）分隔：

```
Header.Payload.Signature
```

- **Header（头部）**：包含算法和类型，如 `{ "alg": "HS256", "typ": "JWT" }`
- **Payload（载荷）**：存放实际需要传递的数据，如 userId、openid
- **Signature（签名）**：由前两部分加上密钥生成，用于验证消息未被篡改

### 3. 实现流程

```
用户登录 → 验证凭证 → 生成JWT → 返回Token → 前端存储
                                            ↓
后续请求 → 携带Token → 后端验证 → 提取用户信息 → 执行业务
```

### 4. 核心代码

#### Token 生成（`server/HomeMediRemind/routes/auth.js`）

```javascript
const token = jwt.sign(
  { userId, openid },        // Payload：用户信息
  config.jwt.secret,          // 密钥：用于签名
  { expiresIn: config.jwt.expiresIn }  // 配置：7天过期
);
```

#### Token 验证中间件（`server/HomeMediRemind/middleware/auth.js`）

```javascript
// 1. 从请求头获取 Token
const authHeader = req.headers.authorization;
const token = authHeader.substring(7); // 移除 "Bearer "

// 2. 验证 Token 有效性
const decoded = jwt.verify(token, config.jwt.secret);

// 3. 从数据库获取完整用户信息（双重保障）
const [users] = await pool.query(
  'SELECT id, openid, nickname, avatar_url FROM users WHERE id = ?',
  [decoded.userId]
);

// 4. 将用户信息附加到请求对象
req.user = { id: users[0].id, openid: users[0].openid, ... };
next();
```

#### 前端 Token 管理（`miniprogram/utils/dataManager.js`）

```javascript
class TokenManager {
  static getToken() {
    return wx.getStorageSync('token') || '';
  }
  static setToken(token) {
    wx.setStorageSync('token', token);
  }
}

// 请求时自动携带 Token
header['Authorization'] = `Bearer ${token}`;
```

### 5. JWT 的优势

- **无状态**：服务器不需要存储 Session，易于水平扩展
- **跨域友好**：Token 放在 Header 中，天然支持跨域请求
- **安全性**：签名防止篡改，过期时间自动失效

---

## 二、家庭权限隔离机制

### 1. 设计目标

- **数据隔离**：不同家庭的用户只能看到自己家庭的数据
- **角色权限**：管理员和成员有不同的操作权限
- **多家庭支持**：一个用户可以属于多个家庭

### 2. 数据库设计

所有业务表都包含 `family_id` 字段：

```sql
CREATE TABLE medicines (
  id BIGINT PRIMARY KEY,
  family_id BIGINT,  -- 关键！数据隔离
  name VARCHAR(255),
  ...
);

CREATE TABLE family_user_roles (
  family_id BIGINT,
  user_id BIGINT,
  role ENUM('admin', 'member'),
  ...
);
```

### 3. 权限中间件（核心）

#### `checkCurrentFamilyAccess` - 检查访问权限

```javascript
const checkCurrentFamilyAccess = async (req, res, next) => {
  const familyId = getCurrentFamilyId(req);
  const userId = req.user.id;

  // 查询用户是否属于该家庭
  const [rows] = await pool.query(
    'SELECT role FROM family_user_roles WHERE family_id = ? AND user_id = ?',
    [familyId, userId]
  );

  if (rows.length === 0) {
    return res.status(403).json({ code: 403, message: '无权访问该家庭' });
  }

  req.familyRole = rows[0].role;
  req.familyId = parseInt(familyId);
  next();
};
```

#### `checkCurrentFamilyAdmin` - 检查管理员权限

```javascript
const checkCurrentFamilyAdmin = async (req, res, next) => {
  const [rows] = await pool.query(
    'SELECT role FROM family_user_roles WHERE family_id = ? AND user_id = ? AND role = ?',
    [familyId, userId, 'admin']
  );

  if (rows.length === 0) {
    return res.status(403).json({ code: 403, message: '需要管理员权限' });
  }
  next();
};
```

### 4. 业务路由中的应用

```javascript
// 查询药品：只需访问权限
router.get('/list', auth, checkCurrentFamilyAccess, async (req, res) => {
  const [rows] = await pool.query(
    'SELECT * FROM medicines WHERE family_id = ?',
    [req.familyId]
  );
});

// 添加药品：需要管理员权限
router.post('/add', auth, checkCurrentFamilyAdmin, async (req, res) => {
  await pool.query(
    'INSERT INTO medicines (id, family_id, name, ...) VALUES (?, ?, ?, ...)',
    [id, req.familyId, name, ...]
  );
});
```

### 5. 角色权限表

| 操作 | 管理员 | 成员 |
|------|--------|------|
| 查看药品列表 | ✅ | ✅ |
| 添加/修改/删除药品 | ✅ | ❌ |
| 创建用药计划 | ✅ | ❌ |
| 查看用药计划 | ✅ | ✅ |
| 记录用药 | ✅ | ✅ |
| 邀请/移除成员 | ✅ | ❌ |

### 6. 完整流程

```
用户登录 → 选择家庭 → 前端存储 familyId → 请求携带 X-Family-Id
→ 后端中间件检查权限 → 查询自动过滤 WHERE family_id = ?
```

---

## 三、定时任务调度

### 1. 技术选型

使用 `node-cron` 库实现定时任务调度。

### 2. 两个定时任务（`server/HomeMediRemind/utils/scheduler.js`）

```javascript
const cron = require('node-cron');

class Scheduler {
  static init() {
    // 药品过期检查（每小时执行一次）
    cron.schedule('* * * * *', async () => {
      await notificationService.checkAndSendExpiryNotifications();
    }, { timezone: 'Asia/Shanghai' });

    // 用药提醒检查（每分钟执行）
    cron.schedule('* * * * *', async () => {
      await notificationService.checkAndSendMedicationNotifications();
    }, { timezone: 'Asia/Shanghai' });
  }
}
```

### 3. Cron 表达式详解

```
┌─────────── 秒 (可选，0-59)
│ ┌───────── 分 (0-59)
│ │ ┌─────── 时 (0-23)
│ │ │ ┌───── 日 (1-31)
│ │ │ │ ┌─── 月 (1-12)
│ │ │ │ │ ┌─ 星期 (0-7)
│ │ │ │ │ │
* * * * * *
```

| 表达式 | 含义 |
|--------|------|
| `* * * * *` | 每分钟执行 |
| `0 8 * * *` | 每天早上 8:00 执行 |
| `0 */2 * * *` | 每2小时执行 |
| `0 9,18 * * *` | 每天 9:00 和 18:00 各执行一次 |

### 4. 启动方式（`server/HomeMediRemind/app.js`）

```javascript
const server = app.listen(port, () => {
  console.log(`Server is running on port ${port}`);
  Scheduler.init();  // 服务器启动后初始化定时任务
});
```

### 5. 用药提醒核心逻辑

```javascript
// 每分钟检查用药提醒
const currentTime = currentHour * 60 + currentMinute;  // 当前分钟数
const slotTime = slotHour * 60 + slotMinute;          // 用药分钟数
const timeDiff = slotTime - currentTime;

// 如果当前时间正好是提前提醒时间
if (timeDiff === reminderTime) {
  // 发送微信通知
  await this.sendSubscribeMessage(user.openid, templateId, messageData, page);
}
```

---

## 四、微信订阅消息推送

### 1. 整体架构

```
用户端：订阅流程 → 后端：保存订阅状态 → 定时任务触发 → 发送微信消息
```

### 2. 数据库设计

#### 用户订阅表 `user_subscriptions`

| 字段 | 说明 |
|------|------|
| `user_id` | 用户 ID |
| `template_id` | 微信模板 ID |
| `template_type` | 模板类型：`expiry` 或 `medication` |
| `is_active` | 是否激活 |

#### 用户通知设置表 `user_notification_settings`

| 字段 | 说明 | 默认值 |
|------|------|--------|
| `reminder_time` | 提前提醒时间（分钟） | 15 |
| `expiry_warning_days` | 过期预警天数 | 30 |

### 3. Access Token 缓存

```javascript
async getAccessToken() {
  // 缓存存在且未过期，直接返回
  if (this.accessToken && Date.now() < this.tokenExpireTime) {
    return this.accessToken;
  }

  // 调用微信 API 获取
  const response = await axios.get('https://api.weixin.qq.com/cgi-bin/token', {
    params: {
      grant_type: 'client_credential',
      appid: config.wechat.appId,
      secret: config.wechat.secret
    }
  });

  // 缓存 110 分钟（微信官方 2 小时，留安全边界）
  this.accessToken = response.data.access_token;
  this.tokenExpireTime = Date.now() + (110 * 60 * 1000);

  return this.accessToken;
}
```

### 4. 发送订阅消息

```javascript
async sendSubscribeMessage(openid, templateId, data, page) {
  const accessToken = await this.getAccessToken();

  const response = await axios.post(
    `https://api.weixin.qq.com/cgi-bin/message/subscribe/send?access_token=${accessToken}`,
    {
      touser: openid,
      template_id: templateId,
      page: page,
      data: data,
      miniprogram_state: 'developer'
    }
  );

  return response.data.errcode === 0 ? { success: true } : { success: false };
}
```

### 5. 用药提醒的消息数据格式

```javascript
const messageData = {
  thing1: { value: plan.medicine_name.substring(0, 20) },  // 药品名称
  time2: { value: slot },                                   // 用药时间
  thing3: { value: plan.member_name.substring(0, 20) },     // 用药人
  thing4: { value: plan.frequency || '按时服用' }           // 服用频率
};
```

### 6. 药品过期提醒的消息数据格式

```javascript
const messageData = {
  thing1: { value: medicine.name.substring(0, 20) },      // 药品名称
  date2: { value: this.formatDate(expiryDate) },            // 过期日期
  number3: { value: medicine.days_to_expiry.toString() },   // 剩余天数
  thing4: { value: '请及时使用或处理' }                     // 提示
};
```

---

## 五、数据库触发与变更日志

### 1. 设计目的

实现多设备间数据实时同步。当一个设备修改数据，其他设备能快速获取变更。

### 2. 数据变更日志表 `data_change_logs`

```sql
CREATE TABLE data_change_logs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    family_id BIGINT NOT NULL,
    table_name VARCHAR(50) NOT NULL,  -- 表名
    record_id BIGINT NOT NULL,        -- 记录ID
    action ENUM('create', 'update', 'delete') NOT NULL,  -- 操作类型
    changed_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 3. 触发器设计

为三个业务表（medicines、plans、family_members）分别创建三个触发器：

```sql
-- medicines INSERT 触发器
CREATE TRIGGER medicines_after_insert
AFTER INSERT ON medicines
FOR EACH ROW
BEGIN
    IF NEW.family_id IS NOT NULL THEN
        INSERT INTO data_change_logs (family_id, table_name, record_id, action)
        VALUES (NEW.family_id, 'medicines', NEW.id, 'create');
    END IF;
END;

-- medicines UPDATE 触发器
CREATE TRIGGER medicines_after_update
AFTER UPDATE ON medicines
FOR EACH ROW
BEGIN
    IF NEW.family_id IS NOT NULL THEN
        INSERT INTO data_change_logs (family_id, table_name, record_id, action)
        VALUES (NEW.family_id, 'medicines', NEW.id, 'update');
    END IF;
END;

-- medicines DELETE 触发器
CREATE TRIGGER medicines_after_delete
AFTER DELETE ON medicines
FOR EACH ROW
BEGIN
    IF OLD.family_id IS NOT NULL THEN
        INSERT INTO data_change_logs (family_id, table_name, record_id, action)
        VALUES (OLD.family_id, 'medicines', OLD.id, 'delete');
    END IF;
END;
```

### 4. NEW vs OLD 关键字

| 关键字 | 说明 | 适用场景 |
|--------|------|----------|
| `NEW` | 新数据（插入/更新后） | INSERT, UPDATE |
| `OLD` | 旧数据（删除/更新前） | DELETE, UPDATE |

### 5. 增量同步接口（`server/HomeMediRemind/routes/sync.js`）

```javascript
router.get('/changes', auth, checkCurrentFamilyAccess, async (req, res) => {
  const { lastSyncTime } = req.query;

  // 获取变更日志
  const [changes] = await pool.query(
    `SELECT id, table_name, record_id, action, changed_at
     FROM data_change_logs
     WHERE family_id = ? AND changed_at > ?
     ORDER BY changed_at ASC`,
    [familyId, syncTime]
  );

  // 整理同步数据：区分 create/update 和 delete
  const syncData = {
    medicines: [],
    plans: [],
    familyMembers: [],
    deletedIds: { medicines: [], plans: [], familyMembers: [] },
    serverTime: new Date().toISOString()
  };

  // 根据 action 类型分别处理
  for (const change of changes) {
    if (change.action === 'delete') {
      syncData.deletedIds[change.table_name].push(change.record_id);
    } else {
      // 查询完整数据并加入同步数据
    }
  }
});
```

### 6. 日志清理

```sql
-- 存储过程：清理7天前的旧日志
CREATE PROCEDURE clean_old_change_logs()
BEGIN
    DELETE FROM data_change_logs WHERE changed_at < DATE_SUB(NOW(), INTERVAL 7 DAY);
END;

-- 定时事件：每天凌晨3点自动执行
CREATE EVENT clean_logs_daily
ON SCHEDULE EVERY 1 DAY
STARTS TIMESTAMP(CURRENT_DATE + INTERVAL 1 DAY, '03:00:00')
DO CALL clean_old_change_logs();
```

### 7. 完整流程

```
数据修改 → 触发器自动记录日志 → 前端调用 /api/sync/changes
→ 查询变更日志 → 获取实际数据 → 更新本地数据
→ 每天凌晨3点自动清理7天前的旧日志
```

---

## 六、前端数据缓存

### 1. 设计目的

减少重复网络请求，提升用户体验，降低服务器压力。

### 2. CacheManager 缓存管理类（`miniprogram/utils/dataManager.js`）

```javascript
class CacheManager {
  static CACHE_PREFIX = 'medicine_cache_';
  static CACHE_EXPIRY = 5 * 60 * 1000; // 5分钟过期

  // 设置缓存
  static setCache(key, data) {
    const cacheData = {
      data: data,
      timestamp: Date.now()  // 保存当前时间戳
    };
    wx.setStorageSync(`${this.CACHE_PREFIX}${key}`, cacheData);
  }

  // 获取缓存（带过期检查）
  static getCache(key) {
    const cacheData = wx.getStorageSync(`${this.CACHE_PREFIX}${key}`);
    if (!cacheData) return null;

    // 检查缓存是否过期
    if (Date.now() - cacheData.timestamp > this.CACHE_EXPIRY) {
      this.removeCache(key);
      return null;
    }

    return cacheData.data;
  }

  // 清除特定家庭的缓存
  static clearFamilyCache(familyId) {
    const keys = wx.getStorageInfoSync().keys;
    keys.forEach(key => {
      if (key.startsWith(`${this.CACHE_PREFIX}${familyId}_`)) {
        wx.removeStorageSync(key);
      }
    });
  }
}
```

### 3. 缓存数据结构

```javascript
{
  data: 实际数据,        // 缓存的数据内容
  timestamp: 保存时间戳   // 用于判断是否过期
}
```

### 4. 实际应用示例

```javascript
// 获取药品列表（先查缓存，没有再请求API）
static async getMedicines() {
  const familyId = FamilyManager.getCurrentFamilyId();
  const cacheKey = `${familyId}_medicines`;

  // 1. 先从缓存获取
  const cachedData = CacheManager.getCache(cacheKey);
  if (cachedData) return cachedData;

  // 2. 缓存没有，调用 API
  const res = await this.request('/medicine/list', 'GET');
  if (res.code === 0) {
    CacheManager.setCache(cacheKey, res);  // 缓存数据
    return res;
  }
}

// 数据变更后清除缓存
static async addMedicine(medicine) {
  const res = await this.request('/medicine/add', 'POST', medicine);
  if (res.code === 0) {
    const cacheKey = `${FamilyManager.getCurrentFamilyId()}_medicines`;
    CacheManager.removeCache(cacheKey);  // 清除缓存
    return res;
  }
}
```

### 5. 缓存流程图

```
用户请求 → 查缓存 → 有且未过期？→ 返回缓存数据
                    ↓ 否
                  调用API → 存入缓存 → 返回数据

数据变更 → 清除对应缓存 → 下次请求重新获取
```

---

## 七、OCR文字识别与药品溯源

### 1. 功能概述

通过拍照识别药品包装盒上的文字信息，自动提取药品名称、规格、用法用量、有效期、生产厂商，以及**药品追溯码**。

### 2. 技术选型

- **OCR服务**：百度AI通用文字识别（高精度版）
- **条码查询**：阿里云市场的条码查询API
- **文件上传**：使用 multer 中间件

### 3. 前端流程（`miniprogram/pages/trace-confirm/trace-confirm.js`）

```javascript
// 用户选择拍照或从相册选择
takeTracePhoto: function(sourceType) {
  wx.chooseImage({
    count: 1,
    sourceType: [sourceType],
    success: (res) => {
      this.recognizeTraceCode(res.tempFilePaths[0]);
    }
  });
},

// 调用后端 API 识别
recognizeTraceCode: function(imagePath) {
  wx.showLoading({ title: '识别中...' });

  DataManager.recognizeImage(imagePath).then(res => {
    if (res.code === 0 && res.data && res.data.traceCode) {
      // 识别成功，跳转到确认页面
      getApp().globalData.traceCodeImage = imagePath;
      wx.navigateTo({
        url: `/pages/trace-confirm/trace-confirm?traceCode=${encodeURIComponent(res.data.traceCode)}`
      });
    }
  });
}
```

### 4. 溯源确认页面（`pages/trace-confirm/trace-confirm.js`）

```javascript
// 确认并查询
onConfirm: function() {
  // 复制追溯码到剪贴板
  wx.setClipboardData({
    data: this.data.traceCode,
    success: () => {
      // 跳转到"码上放心"平台查询
      wx.navigateTo({
        url: '/pages/webview/webview?url=https://www.mashangfangxin.com/'
      });
    }
  });
}
```

### 5. 后端 OCR 识别（`server/HomeMediRemind/services/recognitionService.js`）

#### 获取百度 AccessToken（缓存30天）

```javascript
async getBaiduAccessToken() {
  if (this.baiduAccessToken && Date.now() < this.baiduTokenExpireTime) {
    return this.baiduAccessToken;
  }

  const response = await axios.get(config.baidu.tokenUrl, {
    params: {
      grant_type: 'client_credentials',
      client_id: config.baidu.apiKey,
      client_secret: config.baidu.secretKey
    }
  });

  this.baiduAccessToken = response.data.access_token;
  this.baiduTokenExpireTime = Date.now() + (29 * 24 * 60 * 60 * 1000);
  return this.baiduAccessToken;
}
```

#### 图片识别主方法

```javascript
async recognizeImage(imageBuffer) {
  const accessToken = await this.getBaiduAccessToken();
  const imageBase64 = imageBuffer.toString('base64');

  // 调用百度OCR API
  const response = await axios.post(
    `${config.baidu.ocrUrl}?access_token=${accessToken}`,
    'image=' + encodeURIComponent(imageBase64),
    { headers: { 'Content-Type': 'application/x-www-form-urlencoded' } }
  );

  // 解析识别结果
  return {
    success: true,
    data: this.parseOCRResult(response.data)
  };
}
```

### 6. OCR 结果解析（核心算法）

```javascript
parseOCRResult(ocrData) {
  const words = ocrData.words_result.map(item => item.words);
  const fullText = words.join('\n');

  const result = {
    name: '',
    manufacturer: '',
    specification: '',
    category: '其他',
    dosage: '',
    expiryDate: '',
    traceCode: ''
  };

  // 1. 识别药品名称 - 匹配【药品名称】标签
  const drugNameMatch = fullText.match(/【药品名称】\s*[\s\S]*?通用名称[：:]\s*([^\n]+)/);

  // 2. 识别药品规格 - 匹配【产品规格】标签
  const specMatch = fullText.match(/【产品规格】\s*([^【\[]+)/i);

  // 3. 识别用法用量 - 匹配【用法用量】标签
  const dosageMatch = fullText.match(/【用法用量】\s*([^【\[]+)/i);

  // 4. 识别有效期至 - 匹配【有效期至】标签
  const expiryMatch = fullText.match(/【有效期至】\s*(\d{4}[\.\-\/年]?\d{1,2}[\.\-\/月]?\d{0,2})/i);

  // 5. 识别生产厂商 - 匹配【生产企业】标签
  const mfgMatch = fullText.match(/【生产企业】\s*([^\n]+)/i);

  // 6. 识别药品追溯码 - 优先匹配20位数字
  const traceCodeMatch = fullText.match(/药品追溯码[\s\S]*?(\d{20})/);

  return result;
}
```

### 7. 日期格式化

```javascript
formatDate(dateStr) {
  // 支持多种格式
  // 20280302 → 2028.03.02
  // 2028年03月02日 → 2028.03.02
  // 2028-03-02 → 2028.03.02
  // 2028/03/02 → 2028.03.02
  // 2028.03.02 → 2028.03.02
}
```

### 8. 完整流程图

```
用户拍照 → 上传图片 → 获取百度Token → 调用百度OCR
→ 返回文字数组 → parseOCRResult解析
→ 提取：名称、规格、用法、有效期、厂商、追溯码
→ 返回前端 → 跳转确认页面 → 复制追溯码 → 跳转码上放心平台
```

---

## 八、JSON类型字段处理

### 1. 为什么使用JSON字段

- **结构化数据存储**：用药时间段是一个数组，用JSON存储更自然
- **灵活扩展**：不需要提前定义所有字段
- **减少表关联**：提高查询效率

### 2. 项目中的JSON字段

| 表名 | 字段名 | 数据类型 | 示例值 |
|------|--------|---------|--------|
| `plans` | `time_slots` | 字符串数组 | `["08:00", "12:00", "18:00"]` |
| `recognition_history` | `recognition_result` | JSON对象 | `{name: "阿莫西林", ...}` |

### 3. 存储JSON数据

#### 手动序列化（`services/recognitionService.js`）

```javascript
async saveRecognitionHistory(userId, familyId, type, inputData, result) {
  await pool.query(
    `INSERT INTO recognition_history (user_id, family_id, type, input_data, recognition_result, is_added)
     VALUES (?, ?, ?, ?, ?, FALSE)`,
    [userId, familyId, type, inputData, JSON.stringify(result)]
    // ⭐ 使用 JSON.stringify() 把对象转换为JSON字符串
  );
}
```

#### 自动处理（Express + MySQL）

```javascript
// 前端发送 JSON 请求体（Express自动解析）
router.post('/add', auth, checkCurrentFamilyAdmin, async (req, res) => {
  const { timeSlots } = req.body;  // timeSlots 是数组 ["08:00", "12:00"]
  // MySQL驱动可能自动序列化JSON类型字段
});
```

### 4. 读取JSON数据

```javascript
// sync.js 中的兼容处理
const plansData = plans.map(row => ({
  id: row.id,
  medicineName: row.medicine_name,
  // ⭐ 兼容两种解析情况
  timeSlots: Array.isArray(row.time_slots)
    ? row.time_slots                    // 驱动已自动解析
    : JSON.parse(row.time_slots || '[]'), // 需要手动解析
  ...
}));
```

### 5. 处理流程图

```
前端构造数组 → 发送请求 → 后端接收 → JSON.stringify()存入数据库
                                            ↓
数据库读取 → 判断是数组还是字符串 → 统一转换为数组 → 返回前端
```

---

## 九、密码加密存储

### 1. 为什么需要加密

- **安全风险**：如果数据库被泄露，明文密码直接暴露
- **隐私保护**：用户可能在多个网站使用相同密码
- **合规要求**：法律法规要求安全存储用户密码

### 2. bcrypt 加密算法

Node.js 生态中最常用的密码加密库，特点：
- **加盐自动处理**：每个密码都有唯一的盐
- **计算成本可调**：可以随着硬件提升增加迭代次数
- **单向加密**：无法从哈希值反推原始密码

### 3. 数据库设计

```sql
CREATE TABLE users (
    id BIGINT PRIMARY KEY,
    password VARCHAR(255) COMMENT '密码哈希（bcrypt加密）',
    ...
);
```

### 4. 核心实现

#### 注册时加密密码

```javascript
const bcrypt = require('bcrypt');

// 注册
router.post('/register', async (req, res) => {
  const { nickname, password } = req.body;

  const saltRounds = 10;  // 迭代次数（10-12比较合适）
  const hashedPassword = await bcrypt.hash(password, saltRounds);

  await pool.query(
    'INSERT INTO users (id, nickname, password, ...) VALUES (?, ?, ?, ...)',
    [userId, nickname, hashedPassword, ...]
  );
});
```

#### 登录时验证密码

```javascript
router.post('/login-by-nickname', async (req, res) => {
  const { nickname, password } = req.body;

  // 查找用户
  const [users] = await pool.query(
    'SELECT id, nickname, password FROM users WHERE nickname = ?',
    [nickname]
  );

  // 验证密码
  const isPasswordValid = await bcrypt.compare(password, users[0].password);

  if (!isPasswordValid) {
    return res.status(401).json({ code: 401, message: '用户名或密码错误' });
  }

  // 生成 JWT Token
  const token = jwt.sign({ userId: users[0].id }, config.jwt.secret, { expiresIn: '7d' });
  res.json({ code: 0, message: '登录成功', data: { token, userId: users[0].id } });
});
```

### 5. bcrypt 哈希格式

```
$2b$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy
 \_/ \/ \____________________/\_________________________/
  |  |           |                        |
  |  |           |                        → 哈希值（31个字符）
  |  |           → Salt（22个字符）
  |  → Cost Factor（迭代次数，2^10 = 1024次）
  → 算法版本（$2b）
```

### 6. Cost Factor 选择

| Cost Factor | 迭代次数 | 耗时（大致） | 推荐场景 |
|------------|---------|-------------|--------|
| 10 | 1024 | ~100ms | 普通应用 ✓ |
| 12 | 4096 | ~400ms | 较好安全性 |
| 14 | 16384 | ~1.6s | 高安全性应用 |

### 7. 安全最佳实践

- **使用HTTPS**：防止密码在传输中被截获
- **使用bcrypt**：不要自己写加密算法
- **不存储明文**：永远不要存储明文密码
- **适当迭代次数**：10-12比较合适
- **登录失败限制**：防止暴力破解

---

## 附录：核心文件索引

| 技术要点 | 核心文件 |
|----------|---------|
| JWT认证 | `server/HomeMediRemind/routes/auth.js`, `server/HomeMediRemind/middleware/auth.js`, `miniprogram/utils/dataManager.js` |
| 家庭权限 | `server/HomeMediRemind/middleware/permission.js`, `server/HomeMediRemind/routes/families.js` |
| 定时任务 | `server/HomeMediRemind/utils/scheduler.js`, `server/HomeMediRemind/services/notificationService.js` |
| 微信推送 | `server/HomeMediRemind/services/notificationService.js`, `server/HomeMediRemind/routes/notifications.js` |
| 数据库触发器 | `db/database_migration.sql`, `server/HomeMediRemind/routes/sync.js` |
| 前端缓存 | `miniprogram/utils/dataManager.js` |
| OCR识别 | `miniprogram/pages/trace-confirm/trace-confirm.js`, `server/HomeMediRemind/services/recognitionService.js` |
| JSON处理 | `server/HomeMediRemind/routes/sync.js`, `server/HomeMediRemind/services/recognitionService.js` |
| 密码加密 | `server/HomeMediRemind/routes/auth.js` |
